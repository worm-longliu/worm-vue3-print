export default {
  id: 'faq',
  title: '常见问题',
  content: `
<h2>如何调整纸张大小？</h2>
<ol>
  <li>在右侧面板选择"页面设置"</li>
  <li>选择预设纸张：常用纸张（A4、A3、A5、Letter、Legal）、针式打印纸（全等分 / 二等分 / 三等分）、标签纸（80×60、60×40、40×30）、小票纸（57 / 80 / 110mm）</li>
  <li>或选择"自定义"输入宽度和高度（mm）；"连续纸"可自定义纸宽，出纸高度按内容自动推导</li>
  <li>设置方向：纵向（portrait）或横向（landscape）；切换即交换纸张长宽，设计稿内容随之铺在该尺寸上。连续纸与小票纸固定纵向</li>
  <li>内容旋转角度：0° / 90° / 180° / 270°，整页内容旋转对应角度出纸。90° / 270° 时纸张长宽互换以贴合旋转后的内容，设计稿内容保持原方向、不变形不裁切；0° 为不旋转（默认）。连续纸与小票纸固定纵向、无旋转概念</li>
</ol>

<h2>如何处理分页？</h2>
<p>数据表格支持自动分页：</p>
<ol>
  <li>在表格属性中设置"每页显示行数"</li>
  <li>启用"表头重复"确保跨页时显示标题行</li>
  <li>添加小计行显示每页小计</li>
  <li>添加汇总行显示整表合计</li>
  <li>文本、图片等非表格元素按位置整体移页，不会拆开；已组合的元素整组强制同页，组过高会被裁切（见"如何组合多个元素"）</li>
</ol>

<h2>如何导入外部数据？</h2>
<p>通过组件 props 传入数据：</p>
<ul>
  <li><code>fields</code>：字段定义数组，用于字段树显示</li>
  <li><code>initialTemplate</code>：初始模板数据</li>
</ul>
<p>模板加载 / 重置（如「加载默认布局」）由宿主实现：把新的模板数据赋给 <code>initialTemplate</code> 即可重载画布。</p>

<h2>如何使用表达式绑定字段？</h2>
<ol>
  <li>双击元素打开表达式编辑器</li>
  <li>使用 <code>{field.path}</code> 语法绑定字段</li>
  <li>使用内置函数格式化数据（如 <code>{MONEY(price)}</code>）</li>
  <li>点击确认保存</li>
</ol>

<h2>如何调整元素层级？</h2>
<ol>
  <li>选中一个或多个元素</li>
  <li>点击工具栏的层级按钮（带上下箭头图标）：
    <ul>
      <li>置顶（箭头到顶线）</li>
      <li>上移一层（向上箭头）</li>
      <li>下移一层（向下箭头）</li>
      <li>置底（箭头到底线）</li>
    </ul>
  </li>
</ol>
<p>或使用快捷键在图层面板中调整。</p>

<h2>如何选择多个元素？</h2>
<ol>
  <li>按住 Ctrl（Mac 为 ⌘）在画布或左侧图层面板中逐个点击元素，逐个加入/移出选择</li>
  <li>在画布<strong>空白区域</strong>按下并拖动进行框选（起点落在元素上时会变为选中或拖动该元素，不会启动框选）：
    <ul>
      <li>从左上向右下拖动：与选框<strong>相交</strong>的元素即被选中</li>
      <li>从右下向左上拖动：仅<strong>完全包围</strong>的元素被选中</li>
    </ul>
  </li>
  <li>按 <code>Ctrl + A</code> 全选当前页元素</li>
</ol>

<h2>如何组合多个元素？</h2>
<ol>
  <li>按住 Ctrl（Mac 为 ⌘）逐个点击，或用空白处框选选中多个元素</li>
  <li>点击工具栏"组合"按钮、图层面板底部的组合按钮，或按 <code>Ctrl + G</code></li>
  <li>组合后点击任一成员可选中整组；拖动任一成员整组跟动；打印时整组强制同页</li>
  <li>取消组合：选中组内任一成员后点击"取消组合"（工具栏或图层面板），或按 <code>Ctrl + Shift + G</code></li>
</ol>
<p><strong>注意：</strong>组合元素会强制打印在同一页，引擎不会为它跨页拆分。组合前请确认组内元素的总高度（从最高点到最低点）不超过纸张内容区，否则整组虽会移到新的一页，超出页面的部分仍会被裁掉。组太高时请拆分为多个小组，或取消组合。</p>

<h2>如何使用参考线？</h2>
<ol>
  <li>在标尺区域拖拽添加参考线</li>
  <li>参考线自动参与吸附检测</li>
  <li>拖拽参考线可调整位置</li>
  <li>双击参考线可删除</li>
</ol>

<h2>如何预览打印效果？</h2>
<ol>
  <li>点击工具栏"预览"按钮</li>
  <li>预览使用同构渲染管线，确保与实际打印一致</li>
  <li>可通过"叠层对比"功能对比设计稿与截图</li>
</ol>

<h2>模板数据格式是什么？</h2>
<p>模板以 JSON 格式存储，包含：</p>
<ul>
  <li><code>paperSize</code>：纸张大小</li>
  <li><code>orientation</code>：方向</li>
  <li><code>margins</code>：页边距</li>
  <li><code>header</code>：页眉配置</li>
  <li><code>footer</code>：页脚配置</li>
  <li><code>elements</code>：元素数组</li>
  <li><code>watermark</code>：水印配置</li>
</ul>

<h2>如何调整表格列宽？</h2>
<ol>
  <li>在左侧面板的图层列表中，点击选中对应的表格元素</li>
  <li>此时画布上的表格会显示列宽分隔线</li>
  <li>拖动列宽分隔线即可调整列宽</li>
</ol>
<blockquote>
  <p><strong>注意</strong>：必须先选中表格元素，才能激活列宽拖动功能。</p>
</blockquote>

<h2>如何调整表格属性？</h2>
<ol>
  <li>确保当前没有任何单元格被选中（如有选中，点击画布空白区域取消选中）</li>
  <li>在左侧面板的图层列表中，点击选中对应的表格元素</li>
  <li>右侧面板会显示表格属性设置</li>
</ol>
<blockquote>
  <p><strong>注意</strong>：如果存在选中的单元格，右侧面板将显示单元格属性而非表格属性。</p>
</blockquote>

<h2>为什么部分页面有水印、部分页面没有？</h2>
<ol>
  <li>水印是<strong>按模板</strong>独立设置的，每一个设计的模板都需要单独配置自己的水印信息，不会自动继承其他模板。</li>
  <li>如果出现部分页面存在水印、部分页面没有水印的情况，说明该页面所在的模板没有为自身设置水印。</li>
  <li>为模板添加水印：进入该模板专属页面，在右侧的"页面属性"中找到"水印"配置项进行设计即可。</li>
</ol>
<blockquote>
  <p><strong>提示</strong>：水印配置保存在模板 JSON 的 <code>watermark</code> 字段中，仅对当前模板生效。</p>
</blockquote>
  `
}
