# Security Policy

## 报告安全问题

请勿在公开 Issue 中提交安全漏洞。请通过私信/邮件联系我们（联系信息见仓库主页）。

报告时请包含：

- 影响版本
- 复现步骤
- 影响评估（如 XSS、注入、信息泄露）

## 已知注意事项

- 打印模板为宿主用户自定义内容，HTML 渲染在 Node 侧/浏览器侧；接入方应自行对模板内容做信任边界控制。
- 渲染服务（`services/print-render`）的 `/render/*` 接口使用 `X-Render-Key` 鉴权，请勿在公网以默认密钥暴露。